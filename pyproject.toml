[project]
name = "omp2-extensions"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pyright]
typeCheckingMode = "strict"

[tool.pytest.ini_options]
pythonpath = ["python"]
testpaths = ["python/tests"]
